import { MongoMemoryServer } from 'mongodb-memory-server';

const mongod = new MongoMemoryServer();

export module InMemoryMongoDB {
    export async function start() {
        const uri = await mongod.getUri();
        return uri;
    }

    export async function stop() {
        await mongod.stop();
    }
}